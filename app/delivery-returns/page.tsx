import type { Metadata } from "next";
import { InfoPage, InfoSection } from "@/components/info-page";

export const metadata: Metadata = { title: "Delivery & returns" };
export default function Page() { return <InfoPage eyebrow="The practical bit" title="Delivery & returns." intro="A simple India-only demo policy, designed to be clear before checkout."><InfoSection title="Delivery across India"><p>Standard delivery is shown as 3–6 working days. It is free when the merchandise subtotal reaches ₹1,999; otherwise the demo checkout adds ₹199.</p><p>FashionFunks does not currently create real shipments, request an address or send delivery emails.</p></InfoSection><InfoSection title="Returns"><p>The intended policy allows returns within 15 days of delivery for unworn, unwashed pieces with their original tags attached. Final-sale and hygiene-sensitive items would normally be excluded, though this catalog currently contains clothing only.</p></InfoSection><InfoSection title="Refunds"><p>Because this is a portfolio checkout, no payment is taken and no refund is required. A future production integration would return funds to the original payment method after inspection.</p></InfoSection></InfoPage>; }
